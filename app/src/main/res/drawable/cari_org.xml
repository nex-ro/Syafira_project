<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Warna latar -->
    <corners android:radius="12dp" /> <!-- Radius sudut -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" /> <!-- Garis pinggir opsional -->
</shape>
